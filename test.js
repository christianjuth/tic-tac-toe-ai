const { getBestMove, printBoard, checkWinner, predictWinner } = require('./lib')

// let board = ['','','','','','','','','']
// printBoard(board)

// while (checkWinner(board) === undefined) {
//   board = getBestMove(board)
//   printBoard(board)
//   console.log('prediction=', predictWinner(board))
// }